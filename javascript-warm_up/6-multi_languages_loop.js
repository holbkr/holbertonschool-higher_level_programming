#!/usr/bin/node

/* Print 3 lines but with an array of strings and a loop */

const tableSentences = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; tableSentences[i]; i++) {
  console.log(tableSentences[i]);
}
